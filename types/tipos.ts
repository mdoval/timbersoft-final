export interface IProveedor {
    id?: number,
    nombre: string
}

export interface IUser {
    email: string,
    name: string,
    aserraderoId?: number
}

export interface IAserradero {
    id: number,
    nombre: string    
}